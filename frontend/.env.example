# Auth bypass for local dev (do not use in production)
# off | mock | mock-staff | citizen | staff
# mock = start as citizen; use Citizen/Staff buttons in navbar to switch both roles
VITE_AUTH_BYPASS=off

# API base URL (defaults to /api via nginx proxy)
# VITE_API_BASE_URL=http://localhost:8000/api
